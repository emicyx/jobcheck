# JobCheck — 秋招投递统一追踪平台 设计文档

> 版本 v1.2 · 2026-09-01 · 基于腾讯校准实战 + 接入管线定稿修订（§4/§5 两级模板匹配与免审批发布、§10–§12 决策 12–15）

## 1. 产品定位

为秋招求职者提供的**多用户**岗位投递状态追踪平台：用户把自己投递过的公司官网（招聘门户）接入平台，平台定期自动抓取各门户的投递状态，归一化为统一状态机，在一个看板上集中管理；无法自动抓取的渠道支持手动记录，与自动记录混排展示。

**产品红线（不可协商）**：
1. **只读**：平台仅读取投递状态，绝不提供代投简历、自动投递、自动沟通等写操作功能。
2. 适配器开发规范禁止请求任何写操作端点。
3. 用户可一键注销账号并级联删除全部数据（含 Cookie）。
4. 不存储简历文件，只存投递状态相关字段。

## 2. 核心概念

| 概念 | 说明 |
|---|---|
| **Portal（门户）** | 一个可追踪的招聘系统实例。如「小米校招 = Moka 的 hr.xiaomi.com 实例」。同一供应商模板可派生多个门户 |
| **Binding（绑定）** | 用户与门户的登录态绑定（加密存储的 Cookie/Token），是轮询的单位 |
| **Application（投递）** | 一条岗位投递记录，`source` 分 `auto`（抓取）/ `manual`（手动） |
| **统一状态 / 原文状态** | 平台状态机中的状态 / 官网展示的原始文案（永远保存，用于重放与调试） |

## 3. 总体架构

```
┌─────────────┐   HTTPS    ┌──────────────────────────────┐
│  浏览器插件  │ ─────────▶ │        后端 FastAPI           │
│ (MV3 登录态  │            │  ├─ Web API（看板/绑定/账号）  │
│  捕获+采样)  │            │  ├─ 调度器 APScheduler         │
└─────────────┘            │  │    └─ 抓取引擎             │
                           │  │        ├─ HTTP 客户端(优先) │
┌─────────────┐            │  │        └─ Playwright(降级)  │
│  前端 Vue3   │ ─────────▶ │  ├─ 适配器执行器               │
│  + Naive UI │            │  │    ├─ L1 供应商代码适配器    │
└─────────────┘            │  │    └─ L2 自动配方执行器      │
                           │  └─ 配方生成管线(LLM,一次性)    │
                           └──────────┬───────────────────┘
                                      │
                              SQLite (WAL, 加密备份)
```

- **后端**：Python / FastAPI；SQLite（WAL 模式，50 人规模富余，SQLAlchemy ORM 保留迁 PostgreSQL 的路径）。
- **调度**：APScheduler 进程内调度（无需消息队列），轮询任务按绑定粒度触发、全局并发上限 + 按门户限速。
- **抓取引擎**：双层——API 直连优先（HTTP 客户端 + Cookie 重放各门户的 JSON 接口），JS 渲染且无可用接口的站点降级 Playwright 无头浏览器。
- **前端**：Vue3 + Vite + Naive UI + Pinia + Vue Router，浅色主题（视觉方向实现期用 frontend-design 技能单独设计，不套默认模板）。
- **插件**：Chrome/Edge Manifest V3，开发者模式加载（MVP），负责登录态捕获 + 未支持门户的页面采样（见 §5）。
- **LLM**：仅两个窄用途——配方生成（每门户一次性）与未知状态文案兜底分类（按原文缓存）。日常轮询零 LLM 成本。**实现细节（模型接入、配方 Schema、回放验证器、提示词约束、成本熔断）见 [LLM_DESIGN.md](LLM_DESIGN.md)。**
- **部署**：境内轻量云 2C4G，Nginx + HTTPS（Let's Encrypt），Docker Compose；域名 ICP 备案与开发并行启动。

## 4. 关键流程

### 4.1 接入一个公司（绑定向导）

1. 用户粘贴公司校招官网链接（或搜索内置门户库），平台按**域名规则库**识别门户类型。
2. 识别结果三种：
   - **已支持**（L1 模板实例或已有 L2 配方）→ 进入绑定流程；
   - **未支持但属已知平台**（如另一个 Moka 客户站，域名未登记）→ 引导采样一次，按抓包**结构指纹**自动识别平台并实例化模板，无需管理员；
   - **完全未支持（自研）**→ 进入**自动配方管线**（§5）：引导用户打开该公司"我的投递"页采样；管线失败再转手动模式 + 记入「接入请求」队列。
3. 绑定流程：平台生成一次性 `bind_token` → 用户点「去登录」→ 插件打开目标门户登录页 → 用户正常登录（短信验证码到自己手机，自己输入）→ 插件按该门户配方的 `login_success` 条件检测登录成功 → 通过 `chrome.cookies` 读取目标域 Cookie → 携带 `bind_token` POST 回平台（插件与平台页通信走 `externally_connectable`）。
4. 服务端收到 Cookie 后**立即验证**：真实拉取一次投递列表，成功则绑定激活并完成首次同步；失败则回报用户具体原因（Cookie 无效 / 接口变更 / 需要验证码）。
5. **登录态失效**：每次轮询检测 401 / 跳转登录页 / 配方中的失效特征 → 绑定标记 `expired` → 看板顶部黄条提示「N 个公司需要重新登录」→ 一键重走绑定流程。

### 4.2 状态同步（轮询）

- 频率：每绑定默认 **6 小时**，用户可选 4h / 6h / 12h；任务加随机抖动错峰，偏向夜间低峰窗口。
- 限速：对**同一门户域名全平台**设置速率上限（如 ≤1 次/分钟），50 人规模下单门户日均请求量在数十次级别，风控压力低。
- 链路：抓取 → 归一化（见 §6）→ 与上次快照 diff → 状态变化写入历史表 → 更新看板。
- 失败处理：指数退避重试；连续失败 N 次（默认 5）自动暂停该绑定并标记需重新登录；**遇到验证码不破解**（红线），直接标记转人工重绑。

### 4.3 手动记录与分类

- 不支持门户 / 内推未走系统 / 小程序投递：用户手动添加（公司、岗位、批次、投递日期、当前状态），与自动记录在同一看板混排，`source` 徽标区分。
- 分类维度：**批次**（系统内置：提前批 / 正式批 / 春招 / 实习）、**自定义标签**（用户自建）、来源（自动/手动）、公司筛选。

## 5. 适配器体系 —— 用「自动配方管线」覆盖自研网站长尾

调研结论（2026-09）：腾讯（join.qq.com）/ 网易（campus.163.com）/ 携程（careers.ctrip.com）为自研，小米为 Moka 独立域名部署，去哪儿为飞书招聘。自研站各有 API，不可能一套代码通吃；而**逐站人工抓包写配方对个人运营者不可扩展**（秋招目标公司中自研站约 30–80 家）。因此 L2 层不做人工配方，改为**自动配方管线**：配方由 LLM 从真实采样自动生成，经确定性回放验证后**自动发布，无人工审批**；验证不过即告知用户转手动记录并留存样本（2026-09-01 腾讯实战校准确认了该闭环的可行性，详见 §12 决策 12–15）。

**接入决策流（先模板、后 agent，v1.2 定稿）**：

```
粘贴链接 → ① 域名匹配（portals.domains，零成本）命中 → 直接绑定
  未登记 → 引导采样（DOM + 请求-响应对）
    → ② 结构指纹匹配（本地、免费）：命中平台模板 → 参数化实例化 → 回放验证一次 → 入库 enabled → 绑定
    → 未命中（判定自研）→ ③ agent 生成管线（LLM_DESIGN.md T1）
        验证通过 → 自动发布 → 向导轮询感知 → 用户绑定（绑定即真实拉取，开箱验货）
        验证不过 → 不建门户，提示转手动记录，样本留存供后台干跑重试
```

**四层适配架构**：

| 层 | 机制 | 覆盖对象 | 接入成本 |
|---|---|---|---|
| **L1 供应商代码适配器** | 每供应商一套 Python 类（Moka / 飞书招聘 / 北森 zhiye），门户实例 = 一条配置（slug、域名映射）；未登记的新客户站可由采样包**结构指纹**自动认出并实例化 | Moka（小米、滴滴、B站等 3500+ 客户）、飞书招聘、北森（央国企/金融） | 新增同供应商门户 ≈ 0 成本 |
| **L2 自动配方（LLM 生成）** | 用户在自己投递页触发采样 → 指纹不匹配（判定自研）→ LLM 生成配方 → 回放验证 → 自动发布、全平台复用（管线见下） | 腾讯、网易、携程及一切自研长尾 | 每门户一次性、分钟级，无人值守 |
| **L3 插件本地提取**（后续路线） | 插件在用户浏览器访问投递页时本地提取回传，服务端零请求 | 反爬极强的站点 | MVP 不做 |
| **L4 手动兜底** | 用户手动维护 | 管线失败的长尾 | 0 |

**配方自动生成管线**：

1. **触发**：向导发现门户未支持 → 引导装插件 → 用户正常登录并打开该公司的"我的投递"页 → 点插件上的「接入此网站」。**所有登录用户均可触发**（决策 15），配套治理：同注册域名生成中加锁、已发布配方直接复用（后来者零成本命中）、单门户 24h 冷却（含失败重试）。
2. **采样**：插件**只在该页**采集：渲染后 DOM（裁剪掉 script/style/广告节点）+ 本页的**请求-响应对**（fetch/XHR 包装捕获：URL、方法、参数、JSON 响应体，v1.2 升级——仅有 URL 清单不足以逆向接口结构，腾讯校准已实证）。数据最小化，不碰其他页面与其他数据。
3. **指纹前置（免 LLM）**：采样包先过平台结构指纹库，命中即走 L1 实例化，不进生成环节。
4. **生成**：服务端把「采样包 + 统一状态机定义 + 配方 Schema」交给 LLM，输出配方草稿（JSON）：登录失效判定、列表数据来源（优先 XHR 重放，否则 Playwright + CSS 选择器；单对象响应自动按一条记录处理）、字段提取映射、状态文案初步映射表。**agent 第一版只用采样包，无主动抓取工具**（决策 14）；对方前端才有的数字码语义不猜，留给运行期沉淀。
5. **机器验证**：用采样包回放配方草稿——提取结果必须与 LLM 直接阅读采样包的结论一致，不一致自动修正重试（≤2 次）。这层挡住绝大多数幻觉。附加硬约束：**配方中不得出现采样用户特有标识值**（userId/resumeId 等，与采样用户数据交叉检测），必须参数化为占位符并声明运行时解析方式，否则判失败。
6. **发布沉淀（免审批）**：验证通过即自动发布进门户库（enabled），该公司**所有用户**复用；采样用户绑定即真实拉取，等于开箱验货。验证不过 → 不建门户，向导提示转手动记录，样本留存，管理后台可干跑重试。
7. **持续闭环**：轮询遇到规则表未命中的状态原文 → LLM 分类一次 → 结果写回规则表（标记来源 `llm`，后台可复核）——规则表随使用自动变全，同一原文串永远只调一次 LLM。另一免费纠错源：**用户在详情页手动改状态时，记录「当时的 raw 原文 → 用户改成的统一状态」进映射候选**，人工复核后转正——无人工审批发布下最重要的质量补偿。

**角色分工的本质变化**：管理员（你）从"每站抓包写配方"变为**监控者**——看配方健康度（各门户同步成功率），对过期/失败站点干跑重试迭代提示词；**每个用户自己的登录态就是解锁新门户的钥匙**——你本人投过腾讯/网易/携程，账号现成，首批配方就用你自己的采样生成；配方库是全平台共享的网络效应资产。**模板库边界（决策 13）**：agent 只生成单站配方，自研站配方永不提升为平台模板；指纹库仅人工维护平台条目，自动产物不回流模板库。

**运行成本**：配方生成每门户一次性 1–2 次调用（数万 token 级）；状态分类按原文串缓存；已发布配方的日常轮询**零 LLM 成本**（LLM 的"理解一次"被固化为配方的"确定性执行"）。按 GLM/DeepSeek 档位估算月成本几十元级。长尾配方多为 Playwright + 选择器渲染提取，2C4G 单并发队列可承载（测算：50 用户 × 15 门户 ÷ 6h ≈ 31 次轮询/小时，每次 5–15 秒）。

**准确率兜底**（回应"全自动识别不可信"）：不做运行时裸奔的自动识别，而是**生成时回放验证（唯一硬闸门，免审批）+ 置信度徽标**（看板卡片区分 `已验证配方 / LLM 提取 / 手动`）+ **运行期三重补偿**：同步提取为空/结构异常 → 配方自动标记过期并引导重新采样（不静默给错数据）；用户手改状态沉淀映射（§5 第 7 步）；T2 未知文案兜底分类。

## 6. 统一状态机（细分版）

用户要求尽量细分，最终状态集：

```
进行阶段（有序）：
简历评估中 → 测评中 → 笔试中 → 一面 → 二面 → 三面 → HR面/终面 → 已发Offer → 已入职
（「已投递」原为独立状态，2026-09-02 并入「简历评估中」：归一化几乎总映射到更后阶段，该列长期无数据）

轮次未知兜底：面试中(轮次未知)      ← 官网文案只写"面试安排中"时使用
终态：已拒绝 / 流程终止(岗位关闭) / 已撤回(用户主动) / 已过期
特殊：待确认(显示原文)              ← 规则表与 LLM 兜底都失败时不猜
绑定层状态（不属于岗位）：登录态失效
```

- **规则表映射**：`(供应商|门户) × 原文文案正则 → 统一状态`，按优先级匹配；初始映射由 L2 配方管线随首批采样生成，此后按 §5 第 7 步持续自动沉淀，后台可人工复核。
- **原文永远保存**：每条投递记录 `raw_status_text` + 状态历史表（`from → to`、原文、变更时间）。
- **历史重放**：规则表更新后可对历史 `raw_status_text` 批量重映射——这是细分状态维护成本的兜底机制：初期映射不全没关系，补了规则历史记录也能修正。
- 细分与轮次仅在官网文案可区分时生效，不可区分时落入兜底态，**不虚构轮次**。

## 7. 数据模型（核心表）

```
users            id, email, password_hash(argon2), role(user/admin), invite_code_id, created_at
invite_codes     code, max_uses, used_count, expires_at, created_by
portals          id, name, company, adapter_type(L1_provider/L2_recipe), provider_key,
                 domain_pattern, config(JSON: slug/配方引用), status(enabled/paused), created_at
recipes          id, portal_id, spec(JSON: 认证判定/列表来源/字段提取/状态映射), confidence,
                 status(draft/validated/published/expired), source(auto_gen/manual/admin),
                 version, created_by_sample_id, updated_at
samples          id, portal_id, user_id, payload(JSON: 裁剪DOM+请求-响应对), created_at   ← 配方管线的原料
bindings         id, user_id, portal_id, cookie_encrypted(AES-256-GCM), key_version,
                 status(active/expired/paused), interval_hours, last_check_at, next_check_at,
                 cookie_updated_at, consecutive_failures
applications     id, user_id, portal_id?, binding_id?, source(auto/manual), company, job_title,
                 department?, work_location?, applied_at, batch, current_status, raw_status_text,
                 confidence(recipe/llm/manual), last_synced_at, extra(JSON)
app_status_hist  id, application_id, from_status, to_status, raw_status_text, detected_at
status_rules     id, scope(provider|portal), pattern, mapped_status, priority, source(keyword/llm/manual), enabled
tags             id, user_id, name, color
app_tags         application_id, tag_id
access_requests  id, user_id, url, note, status(pending/done/rejected), admin_note   ← 接入请求
```

## 8. 安全设计

- **Cookie 加密**：AES-256-GCM，密钥独立于代码库（环境变量 → 未来 KMS），支持密钥版本号轮换。
- **会话**：服务端 Session（HttpOnly + Secure Cookie），50 人规模无需 JWT。
- **密码**：Argon2id 哈希。
- **插件回传**：一次性 `bind_token`（短时效、单次有效、绑定发起用户），插件与平台通信域名白名单（`externally_connectable`）；采样功能仅在被用户显式触发的"我的投递"页工作。
- **限频与风控**（同时是对目标网站的礼貌义务）：按门户全局限速、随机抖动、夜间窗口偏好、指数退避、不破解验证码。
- **注销**：级联删除 users → bindings(含 Cookie) → applications → 历史 → 标签。
- 全站强制 HTTPS；管理后台独立权限。

## 9. 前端页面与交互

| 页面 | 内容 |
|---|---|
| 登录/注册 | 邮箱+密码+邀请码 |
| **看板（主视图）** | 按状态分列的看板，卡片 = 公司 + 岗位 + 批次 + 最后同步时间 + 来源徽标（自动[已验证配方/LLM]/手动）+ 待确认标记；顶部黄条提示失效绑定数；筛选器：公司/批次/标签/来源；支持自定义标签；左侧「我的数据」可折叠侧边栏（v0.6.1）：投递总览 / 流程分布（点击筛选看板）/ 个人账号，数据来自 `GET /api/me/stats`，不受看板筛选影响 |
| 投递详情 | 状态时间线（含每步原文）、原始文案展示、手动记录编辑 |
| 接入向导 | 粘贴链接 → 识别门户 →（已支持：插件引导 → 去登录 → 绑定结果）/（未支持：引导采样 → 识别结果确认）；失败转手动记录 + 接入请求 |
| 设置 | 轮询频率（4/6/12h）、标签管理、账号与注销 |
| 管理后台 | 配方健康度监控（同步成功率/过期/失败站点 + 干跑重试）、门户管理、状态规则表（含 llm 来源与手改沉淀候选复核）、接入请求、用户管理、邀请码 |

视觉：**浅色主题**，实现期用 frontend-design 技能定制视觉方向（排版、色板、卡片密度），目标是"每天会打开多次的工具"的清爽感，不做后台管理系统的模板脸。

## 10. MVP 里程碑

> 今天是 2026-09-01，秋招进行中——里程碑按"自己先用起来"排序，每个里程碑结束都是可用状态。

- **M1 手动版（第 1–2 周）**：账号体系（邮箱+密码+邀请码）、手动投递 CRUD、状态看板 + 详情时间线 + 标签/批次、部署骨架。→ 立刻替代 Excel 自用。
- **M2 自动化跑通（第 3–4 周）**：浏览器插件（登录态捕获）、绑定流程、**Moka 适配器（L1）**、调度轮询、状态归一化与历史、失效检测与重绑。→ 小米等 Moka 系公司全自动。
- **M3 上线（第 5–6 周）**：**飞书招聘适配器（去哪儿）**、境内服务器部署 + 备案收尾、管理后台最小版（门户配置 + 用户）。→ 开放给朋友（邀请码）。
- **M4 自研覆盖（第 6 周起持续）**：**自动配方管线**（插件抓请求-响应对 → 两级模板匹配 → LLM 生成 → 回放验证 → 自动发布）；用你自己投过的腾讯/网易/携程账号作为首批采样，验证管线并沉淀前三份自研配方（腾讯 golden 样本已具备：2026-09-01 校准实战产出，含单对象列表与数字码状态两个典型特性）；北森模板视投递需求接入。→ 此后新自研站由用户采样自然长出，你只监控配方健康、对失败站点干跑重试。
- **后续路线**：邮件解析增强（IMAP 授权读通知邮件）、插件本地提取（L3）、配方生成的抓包辅助强化。

## 11. 风险与开放问题

| 风险/未知 | 应对 |
|---|---|
| 腾讯/网易/携程投递页结构未知（需登录态才能看到） | M4 首批采样即逆向过程；目标页导航位置已确认（应聘进度/个人中心/申请记录） |
| LLM 生成配方/分类出错 | 回放验证是唯一硬闸门（免审批，决策 15）；运行期补偿：同步异常自动 expired、用户手改状态沉淀映射、T2 兜底；`llm` 来源规则后台可复核；历史可重放修正 |
| 免审批 + 全员可触发的成本与滥用 | 同注册域名去重 + 单门户 24h 冷却 + 月预算熔断（超限暂停生成、提示暂不支持，不影响已发布配方轮询） |
| 网站改版导致配方失效 | 提取结果为空/结构异常时配方标记 `expired`，看板提示重新采样，不静默给错数据 |
| 各站 Cookie 有效期不一（数天~数周） | 失效检测 + 看板黄条 + 一键重绑；M2 起统计各站实际有效期指导预期 |
| 插件公开分发需商店审核 | MVP 开发者模式加载 + 安装引导文档；上商店列入后续 |
| 单 IP 出口对目标站的风控特征 | 低频（每绑定每日 4–6 次）+ 抖动 + 限速；若个别站仍风控，该站转 L3 路线 |
| Moka `deliver-query` 快捷口每次需短信验证码 | 仅用于「首次批量导入」，持续追踪仍走 Cookie 绑定 |
| SQLite 并发写上限 | 50 人 + 低频轮询远未触及；迁移 PG 路径已预留 |

## 12. 已确认的决策记录

| # | 决策 | 结论 |
|---|---|---|
| 1 | 定位 | 多用户产品，邮箱+密码+邀请码注册 |
| 2 | 范围 | 仅公司官网直投；第三方平台不做 |
| 3 | 登录态获取 | 浏览器插件捕获 Cookie，用户跳转官网正常登录，不手动粘贴 |
| 4 | 自研站覆盖（v1.1 修订） | 四层适配体系；L2 为**自动配方管线**（用户采样 → LLM 生成 → 回放验证 → 确认发布），人工仅兜底审核，不再逐站人工抓包 |
| 5 | 状态机 | 统一状态机 + 尽量细分（测评/笔试分开、面试分轮次），原文保存 + 历史重放 |
| 6 | 刷新 | 服务端定时轮询，默认 6h（可选 4/12h），不主动通知 |
| 7 | 主视图 | 状态看板（Trello 式）；分类维度：批次/标签/来源/公司 |
| 8 | 前端 | Vue3 + Vite + Naive UI，浅色定制设计 |
| 9 | 部署 | 境内轻量云 2C4G，≤50 用户，SQLite |
| 10 | 红线 | 只读、Cookie 加密存储、可注销删除、不破解验证码 |
| 11 | LLM 引入时机（v1.1 提出，v1.2 确认） | 提前引入**窄用途** LLM——配方生成（一次性）+ 未知状态兜底分类（缓存写回规则表）；不做运行时全 LLM 提取 |
| 12 | 模板优先（v1.2 新增） | 新站接入两级匹配：域名表 → 采样包结构指纹（命中平台模板即参数化实例化，免 LLM）；都不中才走 agent |
| 13 | 模板库边界（v1.2 新增） | agent 只生成单站配方，自研站配方**永不提升**为平台模板；指纹库仅人工维护平台条目，自动产物不回流模板库 |
| 14 | agent 能力边界（v1.2 新增） | 第一版**纯采样包分析**（无主动抓取工具）；对方前端才有的数字码语义不猜，靠 T2 与用户手改沉淀补偿 |
| 15 | 触发与审批（v1.2 新增） | 所有登录用户可触发生成，**无管理员审批**；闸门 = Schema 校验 + 回放验证；配套同域去重、单门户 24h 冷却、月预算熔断 |
